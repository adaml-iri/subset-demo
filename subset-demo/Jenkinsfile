pipeline {
    agent any
    stages {
        stage('run ssh tasks') {
            steps {
                sshagent (credentials: ['work_cred']) {
                    sh 'ssh -o StrictHostKeyChecking=no $HOST_IP $CMDS'
                }
            }
        }
    }
}
